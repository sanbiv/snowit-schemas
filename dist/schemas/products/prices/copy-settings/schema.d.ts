export default schema;
declare const schema: {
    type: string;
    properties: {
        priceCodes: {
            type: string;
            title: string;
            description: string;
        };
    };
};
