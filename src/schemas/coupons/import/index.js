import {
  COUPON_TYPE_ENUMS as COUPON_TYPES,
  DEFAULT_COUPON_TYPE,
} from '../types';
import schema from './schema';
import uiSchema from './uiSchema';

export { COUPON_TYPES, DEFAULT_COUPON_TYPE, schema, uiSchema };

export default schema;
